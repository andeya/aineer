# Tracked (committed to the repo)
!AINEER.md
!settings.json
!plugins/
!skills/
!rules/
!specs/
!mcp/

# Ignored (local / runtime artifacts)
settings.local.json
sessions/
agents/
memory/
sandbox-home/
sandbox-tmp/
todos.json
cache/
